# Obsidian 里终于能直接看 CAD 图纸了：.dxf / .dwg 双击即开，不用再切出去开看图软件

项目地址：https://github.com/liamzy2021/Obsidian-CAD-DWG-DXF-viewer
官方社区搜索：CAD Viewer（在 Obsidian 设置 → 第三方插件 → 社区插件市场里搜这个名字也能装）

---
<img width="2176" height="1361" alt="Image" src="https://github.com/user-attachments/assets/b1b91eaa-1142-47fd-8f0c-a2866a179c30" />
## 写在前面

用 Obsidian 记工程/施工资料用了好几年，一直有个特别别扭的地方：笔记里攒了一堆 CAD 图纸，`.dxf` 和 `.dwg` 都有。每次想回看其中一张，都得切出去开 AutoCAD、天正，或者随便哪个看图软件。看着看着，原本笔记的上下文就断了，还得切回来。图一多，切来切去真的很烦。

后来就想，既然图纸都已经在 Obsidian 里了，能不能让它直接把图渲染出来？就像看一张图片那样，双击就开，缩放平移都在笔记里完成。

折腾了一阵，这个插件算是做出来了。

## 一、它到底能干什么

- **在 Obsidian 里直接预览 `.dxf` 和 `.dwg`**：不是把图纸另存成图片再显示，而是真把矢量图渲染出来，放多大都不糊。
- **双击文件即开**：在文件管理器里点 `.dxf` / `.dwg`，直接在笔记标签页里渲染，不用离开 Obsidian。
- **可以缩放、拖拽平移**：滚轮放大缩小，拖着看局部细节，跟在 CAD 软件里看差不多。
- **线条、文字、标注、尺寸都正常显示**：基于开源的 `mlightcad/cad-viewer`，用 Three.js 做 WebGL 渲染，整体观感跟专业看图软件在一个水平上。

## 二、为什么不直接用现成看图软件

说实话，看图软件本身没问题。但笔记的场景不一样：

我是把图纸和对应的施工说明、材料清单、变更记录放在同一个 vault 里的。以前要看某张图，得先最小化 Obsidian，开看图软件，看完再切回来，脑子里那根"这张图对应哪条说明"的线就松了。现在双击直接在笔记里开，旁边就是文字笔记，对照着看，思路是连着的。

对经常要在笔记里引用图纸的人（施工、造价、设计、机电、装修……）来说，这个连续性挺值钱的。

## 三、背后踩的几个坑（顺便记一下）

做的过程里其实挺多事：

- Obsidian 官方安装器默认只下载 `manifest.json` + `main.js` + `styles.css` 这三个文件，子目录里的 Worker 脚本它不会下。结果就是别人装完插件却加载不出渲染引擎。后来我把 Worker 在构建时直接内联进 `main.js`，发布又回到标准三个文件，官方市场装下来就能用。
- DWG 的解析依赖 LibreDWG（GPL 协议），所以整个插件也按 GPL-3.0 开源了，源码都在仓库里。
- 桌面端 Electron 的 Worker 环境有点怪，会误判成 Node.js 导致解析崩溃，绕了一圈才搞定。

这些坑填完之后，DXF 和 DWG 都能稳定打开了。

## 四、目前的限制（诚实说一下）

不想把它说得太满，有几个现实情况得讲清楚：

- **仅支持桌面端**。因为要用到 Electron 的 Web Worker，移动端和网页端暂时跑不了，装了也不会生效。
- **特别复杂的填充图案（hatch）**，个别区域可能不显示填充色，但轮廓线、文字、尺寸标注都是正常的——大多数图纸看下来没感觉，只有那种花里胡哨的填充图偶尔能注意到。
- **大图纸解析要等几秒**，取决于电脑性能和图纸复杂度，不算秒开，但转完了就能流畅看。

## 五、怎么用

两种方式：

1. 在 Obsidian 社区插件市场搜 **CAD Viewer** 直接装（最简单）；
2. 或者去 GitHub 仓库下载 Release 里的三个文件，丢进 `.obsidian/plugins/cad-viewer/` 启用。

装好之后，笔记里任意 `.dxf` / `.dwg` 双击就能看。
